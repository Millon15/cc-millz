{
  "name": "cc-millz",
  "owner": {
    "name": "Millon15"
  },
  "plugins": [
    {
      "name": "agterm-lanes",
      "source": "./plugins/agterm-lanes",
      "description": "Self-labelling agterm lanes for Claude Code — each pane names itself from Claude's own session title, takes a role emoji, tint and sidebar glyph, and re-pins itself so a reboot reattaches the conversation. Requires the agterm terminal (macOS)."
    },
    {
      "name": "codex-delegation",
      "source": "./plugins/codex-delegation",
      "description": "Delegate second-tier work to Codex CLI (gpt-5.6 top tier, gpt-5.5 bulk) — model-routing rubric, Workflow/Agent fan-out wrappers, computer-use verification. Requires the openai-codex plugin."
    },
    {
      "name": "essentials",
      "source": "./plugins/essentials",
      "description": "General-purpose personal skills — code-style (personal code taste) and concise-writing (the compression procedure for comments, commits, PR descriptions and team sends). Install at user scope only."
    },
    {
      "name": "merge-kit",
      "source": "./plugins/merge-kit",
      "description": "Merge-conflict resolution and merge forensics for any repository or multi-repo workspace. /merge-kit:resolve auto-resolves the hunks a stated fact settles and walks the ambiguous ones one at a time; /merge-kit:verify answers whether a merge, rebase or squash silently reverted work that landed on the target after the fork point. The repo map and test command come from a committed .merge-kit.json or are detected from build files; the forge CLI is read from each repo's own origin remote, never from a committed field. Extracted from a private monorepo."
    },
    {
      "name": "phpstorm",
      "source": "./plugins/phpstorm",
      "description": "PhpStorm MCP as an agent surface — code navigation and call hierarchy, inspections, plus a live Xdebug loop (breakpoints, frames, expression evaluation, state mutation) against local or Dockerised PHP. /phpstorm:setup-xdebug drives scripts/xdebug-doctor.sh, which tells three identical-looking debug misconfigurations apart and reads the project's services, ports and path mappings from a committed .xdebug-doctor.json. Requires PhpStorm 2026.2+ with its MCP server enabled. Extracted from a private monorepo."
    },
    {
      "name": "ralphex-revmux",
      "source": "./plugins/ralphex-revmux",
      "description": "revmux as ralphex's external reviewer — live review preflight (codex auth, revmux, profile), the custom_review_script glue (one revmux round per iteration), a two-stage runner that skips ralphex's own review loops, and post-run reporter + optimizer agents. Requires the ralphex and revmux CLIs."
    },
    {
      "name": "revmux-kit",
      "source": "./plugins/revmux-kit",
      "description": "revmux project layer for any repo — config with xhigh-safe timeouts, a profile.md template, and gpt-5.6-sol xhigh / claude fable rosters (sol-panel, sol-final, fable-panel, fable-final). Requires the revmux CLI."
    },
    {
      "name": "security-audit",
      "source": "./plugins/security-audit",
      "description": "Pre-adoption security audit of a repository, package, MCP server or raw script — nine adaptive phases across 21 target types, a terminal verdict plus a full report file with file:line evidence, and a final install/no-install banner. Every external tool is optional: Semgrep when it is on PATH, an IDE search surface when one is up, ripgrep otherwise. Extracted from a private monorepo."
    },
    {
      "name": "statusline",
      "source": "./plugins/statusline",
      "description": "Two-line status line — git scopes for the current repo and nested monorepo repos (branch, dirt, conflicts, ahead/behind, in-progress rebase/merge), context window, skills and MCP schemas actually loaded this session, plus real quota and spend from the Anthropic usage API. Self-installs into settings.json."
    },
    {
      "name": "toolsmith",
      "source": "./plugins/toolsmith",
      "description": "The lifecycle of an agent dev tool. /toolsmith:create authors a skill, command, subagent, rule, script or hook; /toolsmith:check reviews one read-only; /toolsmith:retire removes it from the single place that owns it; /toolsmith:man prints tldr help for anything installed. No command names a directory: scripts/toolsmith.sh --explain detects the project's agent-config layout from a positive marker (a rulesync config, a plugin manifest, or AGENTS.md and its siblings) and every phase reads its paths off that JSON. The reuse search runs over five tiers before anything is authored, and both companion skills are soft — each phase carries its own inline fallback. Extracted from a private monorepo."
    },
    {
      "name": "pstack",
      "source": {
        "source": "git-subdir",
        "url": "https://github.com/cursor/plugins.git",
        "path": "pstack"
      },
      "strict": false,
      "description": "poteto (Lauren Tan)'s pstack, mirrored from cursor/plugins — /poteto-mode playbooks, /how, /why, /interrogate, /arena, /swarm, /unslop, /tdd, principle-* leaf skills. Cursor-authored: model panels (gpt/grok slugs), ~/.cursor/rules override and cursor-team-kit refs do not apply in Claude Code."
    },
    {
      "name": "unslop-kit",
      "source": "./plugins/unslop-kit",
      "description": "Millon15's reply contract as a Russian doll over pstack's unslop and umputun's writing-style: pass 1 pstack:unslop cuts the AI tells, pass 2 review:writing-style pins every claim to an exact reference, pass 3 unslop-formatting lays the reply out as figure-paragraphs (English Check first, TL;DR, emoji claim line, captioned hand-drawn ASCII visual, small numeric table, blockquote receipt ledger, no nested lists, no mermaid source). A SessionStart hook loads it, opt-in via ~/.claude/unslop-kit.mode. Requires pstack@cc-millz and review@umputun-cc-thingz. Personal taste, user scope."
    }
  ]
}
