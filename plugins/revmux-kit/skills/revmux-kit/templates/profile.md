# <project> — project profile for every revmux round

- Repo layout: <one line — monorepo? services? where the entry points live>.
- Stack: <language, framework, versions>.
- The failure that matters most: <what going wrong costs — money, data, uptime>.
- Deliberate conventions (never findings): <patterns that look wrong but are chosen — legacy style kept on purpose, generated code, vendored dirs>.
- What ran before the review and passed: <tests, linters, type checks>. Do not re-run them.
- Rubric: <path or command that prints the project's review checklist, if any>.
- Real failure looks like: <2–4 concrete shapes a reviewer should hunt for>.
