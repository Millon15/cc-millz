---
name: {tool-name}
description: >-
  Use when {trigger condition / the phrases a future session would type} —
  {what the skill provides}. {ALL discovery phrases live here: name, description
  and nothing else is guaranteed to be read.}
---
# {Tool Name}

> **Purpose**: {one-line}.

## When to Use

- {scenario 1}
- {scenario 2}

## {Core Section}

{tables over paragraphs; commands, paths, decision trees}

<!-- A plugin's files are copied to a cache directory on install, so every path
     to a shipped script is ${CLAUDE_PLUGIN_ROOT}/scripts/<name>.sh — never a
     path relative to the consuming project. -->
