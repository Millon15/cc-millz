# A Makefile carrying a `test` target BESIDE a package.json that also declares
# one. The ladder must take this rung and report `detected:makefile`, which is
# what proves the rungs are ordered rather than merely present.

.PHONY: test build

test:
	@echo 'make-repo tests'

build:
	@echo 'make-repo build'
